- content_for :title do
  New Course | Clickx

.page-header
  %h3
    Add New Course for #{@resource_type.titleize}
    %small.pull-right
      %a.btn.btn-sm.btn-reset-sm.btn-warning.btn-reset-m-0{href: super_admin_list_courses_path(@course.resource_type) } Back
.agency-form
  = form_for @course, url: super_admin_courses_path, method: :post do |f|
    = render 'form', f: f
