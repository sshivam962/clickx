.container-fluid.mt-md.pt-xs.ui-2020
  .panel.panel-bg
    .panel-body.ls_form
      = form_for @lead_strategy, url: agency_lead_strategy_path(@lead, @lead_strategy), method: :patch do |f|
        = render 'agency/lead/strategies/shared/form', f: f, lead: @lead, strategy_items: @strategy_items
  .spacer50

