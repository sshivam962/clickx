%p
  %h4
    Package subscription upgraded:
    = @package_name

%p
  Subscription upgraded from #{(@previous_package && @previous_package[:package_name].humanize) || 'Free'} to #{@package_name}

%p
  Amount :
  = @package.amount

%p
  Agency :
  = @agency.name
