!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv": "Content-Type"}
    %meta{charset: "utf-8"}
    %meta{content: "IE=edge", "http-equiv" => "X-UA-Compatible"}
    %meta{content: "width=device-width, initial-scale=1", name: "viewport"}
    %meta{content: 'noindex', name: 'robots'}
    %title= yield(:title) || 'Clickx'
    = stylesheet_link_tag 'signup', media: 'all', 'data-turbolinks-track': true
    = javascript_include_tag "https://maps.googleapis.com/maps/api/js?key=#{ENV['GOOGLE_MAP_KEY']}&libraries=places"
    = javascript_include_tag 'signup', 'data-turbolinks-track': true
    = yield(:custom_assets)
    = csrf_meta_tags

    %script{:src => "https://cdn.linkmink.com/lm-js/2.3.1/lm.js"}
    :javascript
      if (typeof LinkMink==="function") {
        LinkMink("#{ENV['LINKMINK_PUB_LIVE_API_KEY']}").initTracking().then();
      }

    / HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries
    / WARNING: Respond.js doesn't work if you view the page via file://
    /[if lt IE 9]
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>

  %body.focused-form.animated-content
    - if @signup_link&.expires_on.present? && !@signup_link&.expired?
      = render 'shared/countdown_bar', signup_link: @signup_link
    .text-center
      = render 'shared/alerts' if flash.present?
    = yield
    = render 'shared/google_analytics'

    :javascript
      var lm = LinkMink("#{ENV['LINKMINK_PUB_LIVE_API_KEY']}")
